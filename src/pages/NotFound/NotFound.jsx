import Layout from "../../components/Layout";
import MainError from "./MainError";

const NotFound = () => {
  return (
    <Layout>
      <MainError />
    </Layout>
  );
};

export default NotFound;
